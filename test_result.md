# Test notes

Integration tests for the tenant API live in `backend/tests/test_tenant_apis.py`.

Point them at a running server by setting `EXPO_PUBLIC_BACKEND_URL` in `frontend/.env` (see `frontend/.env.example`) or by exporting that variable.
